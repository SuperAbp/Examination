import { Routes } from '@angular/router';
import { ExamManagementExamComponent } from './exam/exam.component';

export const routes: Routes = [{ path: 'exam', component: ExamManagementExamComponent }];
